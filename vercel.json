{
  "$schema": "https://vercel.com/schema",
  "buildCommand": "cd www && yarn install && yarn build",
  "outputDirectory": "www/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/www/$1"
    }
  ]
} 